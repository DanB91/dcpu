; ASSERTGE compares the values in registers A and B.
; Panics and stops the runtime if A < B.
; 
; ## Version History:
;   0.1.0: Initial implementation for spec 1.7.
;
:assertge
  ifl a, b
     panic assertge_str
  set pc, pop

:assertge_str
  dat "Assertion failed: A < B", 0
