; ASSERTLT compares the values in registers A and B.
; Panics and stops the runtime if A >= B.
; 
; ## Version History:
;   0.1.0: Initial implementation for spec 1.7.
;
:assertlt
  ifl a, b
    set pc, pop
  panic assertlt_str

:assertlt_str
  dat "Assertion failed: A >= B", 0
